# 🗺️ 구현 계획서: [작업 명칭]

> **작성 시점:** [날짜]
> **기반 리서치:** `research.md` 참조
> **상태:** 🔴 작성 중 | 🟡 리뷰 대기 | 🟢 승인됨 — 구현 진행 중 | ✅ 완료

---

## 1. 구현 목표 및 범위

### 목표 (Goal)
- [최종적으로 달성하고자 하는 결과물을 구체적으로 정의]
- [사용자 관점에서 "이것이 되면 성공"이라고 볼 수 있는 기준]

### 범위 (Scope)

| 구분 | 내용 |
|------|------|
| **포함 (In Scope)** | [이번 작업에서 구현할 것들] |
| **제외 (Out of Scope)** | [이번에는 하지 않을 것들 — 향후 과제] |

### 완료 기준 (Definition of Done)
- [ ] 기능이 정상 동작한다
- [ ] 타입 체크(`tsc --noEmit` 등)가 통과한다
- [ ] 기존 기능이 깨지지 않는다 (회귀 없음)
- [ ] [프로젝트별 추가 기준이 있다면 여기에]

---

## 2. 상세 구현 단계 (Step-by-Step)

> **규칙:**
> - 각 단계 완료 시 `[ ]`를 `[x]`로 표시하며 진행합니다.
> - 매 단계 완료 후 타입 체크를 수행합니다.
> - 문제가 발생하면 즉시 멈추고 사용자에게 보고합니다.

### Phase 1: 준비 (Setup)

- [ ] **Step 1.1: [환경/구조 준비]**
  - 수정 파일: `path/to/file1.ts`
  - 작업 내용:
    ```
    [구체적인 변경 사항 — 의사코드 또는 실제 코드 스니펫]
    ```
  - 체크포인트: [이 단계가 성공했는지 확인하는 방법]

### Phase 2: 핵심 구현 (Core Implementation)

- [ ] **Step 2.1: [핵심 로직 구현]**
  - 수정 파일: `path/to/file2.ts`
  - 작업 내용:
    ```typescript
    // 변경 전 (Before)
    function oldFunction() { ... }

    // 변경 후 (After)
    function newFunction() { ... }
    ```
  - 의존성: Step 1.1 완료 필요
  - 체크포인트: [확인 방법]

- [ ] **Step 2.2: [추가 로직 구현]**
  - 수정 파일: `path/to/file3.ts`
  - 작업 내용:
    ```
    [구체적인 변경 사항]
    ```
  - 체크포인트: [확인 방법]

### Phase 3: 연결 및 통합 (Integration)

- [ ] **Step 3.1: [컴포넌트/모듈 연결]**
  - 수정 파일: `path/to/file4.ts`
  - 작업 내용:
    ```
    [import 추가, 함수 호출 연결 등]
    ```
  - 체크포인트: [확인 방법]

### Phase 4: 검증 및 안정화 (Verification)

- [ ] **Step 4.1: 타입 체크 실행**
  - 명령어: `npx tsc --noEmit` (또는 프로젝트에 맞는 명령어)
  - 예상 결과: 에러 0건

- [ ] **Step 4.2: 기능 동작 확인**
  - 확인 항목:
    - [ ] [시나리오 1: 정상 케이스]
    - [ ] [시나리오 2: 엣지 케이스]
    - [ ] [시나리오 3: 에러 케이스]

- [ ] **Step 4.3: 회귀 테스트**
  - 명령어: `npm test` (또는 프로젝트에 맞는 명령어)
  - 확인: 기존 테스트 전부 통과

---

## 3. 수정 파일 요약 (File Change Summary)

> 한눈에 어떤 파일이 어떻게 바뀌는지 파악할 수 있는 테이블입니다.

| # | 파일 경로 | 변경 유형 | 변경 내용 요약 |
|---|----------|----------|--------------|
| 1 | `path/to/file1.ts` | 수정 (Modify) | [한 줄 요약] |
| 2 | `path/to/file2.ts` | 신규 (Create) | [한 줄 요약] |
| 3 | `path/to/file3.ts` | 삭제 (Delete) | [한 줄 요약] |

---

## 4. 기술적 결정 사항 (Trade-offs)

> 구현 과정에서 내린 기술적 선택과 그 이유를 기록합니다.

### 결정 1: [결정 제목]
- **선택:** (예: 라이브러리 A 대신 직접 구현)
- **대안:** (예: 라이브러리 A 사용)
- **선택 이유:** (예: 번들 사이즈 최적화, 커스터마이징 용이성)
- **트레이드오프:** (예: 초기 구현 비용 증가, 유지보수 부담)

### 결정 2: [결정 제목]
- **선택:**
- **대안:**
- **선택 이유:**
- **트레이드오프:**

---

## 5. 롤백 계획 (Rollback Plan)

> 구현 중 치명적 문제가 발생했을 때의 복구 전략입니다.

- **롤백 트리거:** [어떤 상황이면 롤백을 결정하는지]
- **롤백 방법:**
  ```bash
  # 예시: git을 이용한 롤백
  git stash        # 현재 변경 사항 임시 저장
  git checkout .   # 모든 변경 되돌리기
  ```
- **데이터 복구:** [DB 변경이 있었다면 복구 방법]

---

## 6. 💡 인간 리뷰 및 피드백 (중요!)

> **🎯 사용자 액션 필요:**
> 아래 공간에 직접 메모(주석)를 달아 클로드에게 피드백을 전달하세요.
>
> **메모 작성 예시:**
> - "Step 2.1의 함수명은 `get-` 대신 `fetch-`로 바꿔줘"
> - "여기엔 캐싱이 필요 없어, 삭제해"
> - "Phase 2 전체를 다른 접근법으로 바꿔줘: [설명]"
> - "이 파일은 건드리지 마"

### 📝 사용자 메모:

<!--
  여기에 메모를 작성하세요. 예:
  - Step 1.1: OK
  - Step 2.1: 함수명을 processData로 변경해줘
  - Step 2.2: 이 단계는 불필요해, 삭제해줘
  - 전체: 에러 핸들링 방식을 try-catch 대신 Result 타입으로 바꿔줘
-->

- [메모 대기 중...]

---

## 7. 구현 로그 (Implementation Log)

> 실제 구현 중 발생한 이슈나 변경 사항을 실시간으로 기록합니다.

| 시간 | 단계 | 내용 | 상태 |
|------|------|------|------|
| [HH:MM] | Step X.X | [작업 내용 또는 발생한 이슈] | ✅ / ⚠️ / ❌ |
| | | | |

---

**⚠️ 경고: 사용자의 명시적 승인 전까지 위 계획을 바탕으로 코드를 직접 작성하지 마라.**

> **승인 방법:** 사용자가 섹션 6에 메모를 남기고, 이 경고 아래에 "승인" 또는 "GO"라고 적으면 구현을 시작합니다.
>
> 승인 상태: [ ] 미승인 / [ ] 조건부 승인 / [ ] 전체 승인
