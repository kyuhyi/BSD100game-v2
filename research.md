# 🔍 리서치 보고서: [작업 명칭을 적으세요]

> **작성 시점:** [날짜]
> **작성자:** Claude Code (바이브 코딩 워크플로우)
> **상태:** 🔴 작성 중 | 🟡 리뷰 대기 | 🟢 승인 완료

---

## 1. 분석 개요

| 항목 | 내용 |
|------|------|
| **분석 대상** | (예: `/src/api/auth` 폴더, Firebase Messaging 로직, DB 스키마 등) |
| **분석 목표** | [수정/추가하려는 기능]을 구현하기 위해 필요한 모든 맥락 파악 |
| **관련 이슈/티켓** | (예: GitHub Issue #42, Jira PROJ-123 등) |
| **예상 난이도** | 🟢 낮음 / 🟡 보통 / 🔴 높음 |

---

## 2. 코드베이스 심층 분석 (Deep Dive)

### 2-1. 핵심 데이터 흐름
> 데이터가 어디서 시작해서 어디로 흐르는지 상세히 기술합니다.

```
[시작점] → [처리 단계 1] → [처리 단계 2] → [최종 목적지]
예: 사용자 입력 → API Route Handler → Service Layer → Database → Response
```

- **입력(Input):** [어떤 데이터가 어디서 들어오는지]
- **처리(Process):** [데이터가 어떻게 변환/가공되는지]
- **출력(Output):** [최종적으로 어디에 저장되거나 반환되는지]

### 2-2. 주요 컴포넌트 / 함수 목록

| 파일 경로 | 함수/컴포넌트명 | 역할 | 비고 |
|-----------|----------------|------|------|
| `path/to/file.ts` | `functionName()` | [이 함수가 하는 일] | [호출 빈도, 중요도 등] |
| `path/to/component.tsx` | `<ComponentName />` | [이 컴포넌트의 책임] | [props, state 구조] |
| | | | |

### 2-3. 기존 설계 패턴 및 컨벤션

- **아키텍처 패턴:** (예: MVC, Clean Architecture, Layered, Feature-based 등)
- **상태 관리:** (예: React Context, Zustand, Redux, 전역 변수 등)
- **네이밍 컨벤션:** (예: camelCase 함수, PascalCase 컴포넌트, kebab-case 파일 등)
- **에러 처리 방식:** (예: try-catch, Error Boundary, 커스텀 에러 클래스 등)
- **테스트 전략:** (예: Jest 단위 테스트, Playwright E2E, 테스트 없음 등)

---

## 3. 의존 관계 및 사이드 이펙트 체크

### 3-1. 상위/하위 의존성 맵

```
[수정 대상 모듈]
  ├── 상위 의존 (이 모듈을 호출하는 곳)
  │   ├── moduleA.ts  ← 영향 받음
  │   └── moduleB.ts  ← 영향 받음
  └── 하위 의존 (이 모듈이 호출하는 곳)
      ├── utilC.ts     ← 변경 불필요
      └── serviceD.ts  ← 시그니처 변경 필요
```

### 3-2. DB / API 영향도

| 구분 | 현재 상태 | 변경 필요 여부 | 상세 내용 |
|------|----------|--------------|----------|
| DB 스키마 | [현재 테이블/컬렉션 구조] | ✅ 필요 / ❌ 불필요 | [마이그레이션 내용] |
| API 엔드포인트 | [현재 라우트 구조] | ✅ 필요 / ❌ 불필요 | [신규/수정 엔드포인트] |
| 외부 API 연동 | [사용 중인 외부 서비스] | ✅ 필요 / ❌ 불필요 | [키/설정 변경 사항] |

### 3-3. 잠재적 위험 요소 (⚠️ 비싼 실패 지점)

> 수정 시 망가질 가능성이 있는 핵심 포인트들을 명시합니다.

1. **위험 요소 1:** [설명]
   - 발생 조건: [어떤 상황에서 문제가 생기는지]
   - 영향 범위: [문제 발생 시 어디까지 영향을 주는지]
   - 완화 방안: [이 위험을 줄이기 위한 조치]

2. **위험 요소 2:** [설명]
   - 발생 조건:
   - 영향 범위:
   - 완화 방안:

---

## 4. 기존 코드 스니펫 (참고용)

> 수정 대상이 되는 핵심 코드를 여기에 붙여넣어 plan.md 작성 시 참고합니다.

```typescript
// 파일: path/to/file.ts (라인 XX-YY)
// 현재 코드
function existingFunction() {
  // ...
}
```

---

## 5. 리서치 결론

### 핵심 발견 사항
- [ ] [발견 1: 현재 시스템의 한계점이나 기술 부채]
- [ ] [발견 2: 구현 시 반드시 지켜야 할 제약 조건]
- [ ] [발견 3: 활용 가능한 기존 인프라/유틸리티]

### 구현 접근 방향 제안
> plan.md 작성 전, 큰 방향성을 먼저 제시합니다.

- **접근법 A:** [설명] → 장점: / 단점:
- **접근법 B:** [설명] → 장점: / 단점:
- **권장 접근법:** [A 또는 B] — 이유: [근거]

---

> **📌 다음 단계:** 이 리서치를 기반으로 `plan.md`를 작성합니다.
> **⚠️ 경고:** 이 리서치가 사용자에 의해 검토/승인되기 전까지 plan.md 작성을 시작하지 마세요.
